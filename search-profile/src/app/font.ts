import { Mogra, Roboto } from "next/font/google";

export const mogra = Mogra({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const roboto = Roboto({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
